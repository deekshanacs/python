                   NUMPY

LOW:
Write a function elementwise_addition that takes two numpy arrays of the same shape and returns a new array that is the element-wise sum of the input arrays.


import numpy as np
def elementwise_add(array1, array2):
    if array1.shape != array2.shape:
        raise ValueError("The input arrays must have the same shape")
    return np.add(array1, array2)
array1 = np.array([[1, 2, 3], [4, 5, 6]])
array2 = np.array([[7, 8, 9], [10, 11, 12]])
result = elementwise_add(array1, array2)
print(result)

